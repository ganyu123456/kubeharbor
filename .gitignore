# Harbor 离线镜像包（体积过大，由 CI 从官方下载）
harbor/harbor.v*.tar.gz
harbor/*.tar.gz
harbor.zip

# Harbor 运行时生成的配置和数据（不应提交）
harbor/common/config/
harbor/docker-compose.yml
harbor/harbor.yml
harbor/harbor.yml.bak
harbor/input/

# 本地测试数据目录
harbor-data/
